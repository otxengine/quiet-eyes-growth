import { Router } from 'express';

const router = Router();

// Placeholder — social publishing endpoints are handled via oauth.ts and agent executors.
// This router is kept for future social-specific REST endpoints.

export default router;
